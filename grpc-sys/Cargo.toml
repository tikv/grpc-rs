[package]
name = "grpc-sys"
version = "0.1.0"
build = "build.rs"

[dependencies]
libc = "0.2"
clippy = { version = "*", optional = true }

[build-dependencies]
gcc = "0.3"
cmake = "0.1"
pkg-config = "0.3"

[features]
default = []
link-sys = []
dev = ["clippy"]
